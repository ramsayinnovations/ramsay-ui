import tw from "twin.macro";

const textarea = {
  wrapper: tw`mt-1 focus:ring-indigo-500 focus:border-indigo-500 border-gray-300 rounded-md`,
  base: tw`shadow-sm mt-1 block w-full sm:text-sm  resize-none`,
};

export default textarea;
